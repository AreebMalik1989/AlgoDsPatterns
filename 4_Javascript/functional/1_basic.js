// Unary
const id  = x => x;
const inc = x => x + 1;
const dec = x => x - 1;

// Binary
const add = a => b => b + a;
const sub = a => b => b - a;
const mul = a => b => b * a;
const div = a => b => b / a;
const mod = a => b => b % a;
